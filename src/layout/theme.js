import { colors } from '../utils/colors';
import { device } from '../utils/mediaSize';

export const theme = {
	colors,
	device,
	font: {
		thin: 300,
		regular: 400,
		bold: 800,
	},
	fontSize: {
		small: '12px',
		regular: '14px',
		large: '16px',
	},
};
